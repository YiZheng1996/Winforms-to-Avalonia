# 决策记录 0003：阶段 3 产品与配方（后端能力）

- 日期：2026-09-01
- 状态：后端完成；配方中心 UI（P1）等待阶段 1 详细规格

## 1. 范围

按总体方案阶段 3 实现“产品、型号、项点、参数定义和配方版本”的后端用例与持久化，
不包含配方中心页面（P1，受 P0 规格门禁约束）。

## 2. 新增能力

- ProductService：创建产品类型/型号、唯一代码校验、停用（停用后 TaskService 拒绝新建任务）、权限 ManageProducts。
- TestDefinitionService：创建试验项/参数、执行器代码必填、枚举参数必须提供可选值、范围校验、权限 ManageTestDefinitions。
- RecipeService 扩展：参数值 upsert（ReplaceParameterValueAsync，ON CONFLICT）、移除项点（级联删除参数值）、
  草稿重命名、报表模板设置、发布校验、复制新版本、停用。
- 仓储：产品/项点/参数 Update、RecipeItem Update/Delete、RecipeParameterValue Replace。

## 3. 退出条件核对（后端部分）

- [x] 可以创建并发布一版有效配方（Core + Integration 证据）
- [x] 发布后不可原地修改、修改必须复制新版本（测试覆盖）
- [x] 参数值通过 ParameterDefinition 校验，不能在运行时临时覆盖（测试覆盖）
- [x] 停用产品/型号后不可用于新任务（TaskService 校验 + 测试）
- [ ] 配方中心 UI 与权限测试（阶段 1 规格后实施）

## 4. 证据

- Core.Tests 36/36、Integration.Tests 12/12、App.Tests 6/6、Headless 2/2（Release）
- 全解决方案 Release 构建 0 警告 0 错误
