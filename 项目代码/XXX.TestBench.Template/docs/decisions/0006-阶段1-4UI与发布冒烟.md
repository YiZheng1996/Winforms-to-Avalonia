# 决策记录 0006：阶段 1-4 UI 页面、测试扩充与 Windows 发布冒烟

- 日期：2026-09-01
- 状态：P0/P1/P2 页面已实现并通过测试；Windows 发布冒烟通过；Hardware/UOS 明确挂起

## 1. 范围

按 0005 规格实现应用 UI（登录/外壳/任务管理/试验执行/配方中心/数据与报表/运行总览/
工艺监控/设备与校准/系统管理/日志诊断），扩充 App.Tests 与 Headless，并完成 Windows 发布冒烟。

## 2. UI 实现要点

- ShellViewModel：登录/首次强制改密/退出、权限过滤导航（按角色隐藏页面）、当前页切换、模式徽标与故障横幅。
- RelayCommand（async + busy）、PageViewModel 基类；组合根 AppComposition 显式装配 ShellServices，ViewModel 不依赖全局服务定位器。
- 页面：所有页面均有真实绑定与可测行为；工艺监控/设备校准写入走 DeviceWritePipeline（权限/模式/活动任务/风险确认）。
- 配方中心：6 个 Tab（产品类型/产品型号/试验项/参数/配方版本/配方编辑器），写操作权限 Manage*。

## 3. 验证证据

- Release 全解决方案构建：0 警告 / 0 错误。
- Release 测试：Core 45、Integration 13、App 9、Headless 1（合计 68）全部通过。
- Windows 发布冒烟：dotnet publish -c Release -r win-x64（框架依赖）→ exe 启动 6 秒存活，
  自动创建 data\testbench.db（SQLite schema 初始化）与日志后正常关闭（证据见 docs\evidence）。

## 4. 挂起项（外部环境依赖，未伪造完成）

- 阶段 5 Hardware：无真实台架/协议适配器，DeviceRuntimeFactory 对 Hardware 明确失败并进入 Faulted，
  写入安全链已就绪；接入点在 IDeviceRuntimeFactory / DeviceWritePipeline / config\device.json。
- 阶段 6 UOS：无 UOS 环境，未做 UOS 构建/启动/现场验收；仅完成 Windows 框架依赖发布冒烟。
- 现场验收：未进行；Simulation/Windows 结果不得视为硬件或现场验收。

## 5. 已知说明

- Avalonia.Headless 多测试会话存在会话隔离问题，Headless 采用单个端到端 AvaloniaFact 覆盖
  故障横幅/模式徽标/登录导航；其余行为由 App.Tests（VM + 真实 Infra）覆盖。
