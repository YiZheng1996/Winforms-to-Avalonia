# 决策记录 0008：第二轮优化（事务/实时/迁移/安全/工程化）

- 日期：2026-09-01

## 已实施
1. 事务原子性：新增 IUnitOfWorkFactory + SqliteUnitOfWorkFactory；TaskService.StartAsync、
   RecipeService.NewVersionFromAsync、TestExecutionService.CompleteAsync 在注入工厂时用
   SqliteUnitOfWork 包裹（提交失败回滚，不留半成品）；组合根已装配。
2. 实时刷新与重试：工艺监控/设备校准视图 1s DispatcherTimer 自动刷新；数据与报表增加“重试/重新生成”按钮。
3. SQLite 版本化迁移：InitializeAsync 按 PRAGMA user_version 依次应用 Migrations（当前 v1，
   后续版本追加元组即可）；新增跨配置校验 ConfigurationValidator（Hardware 必须有启用设备；
   Simulation 初值/规则/故障注入地址必须在 points.json），组合根启动时调用。
4. 安全增强：密码策略——非空即可（2026-09-02 取消 8 位长度与字母数字复杂度限制）；服务层 Ensure 统一记录 AccessDenied 审计
   （TaskService/RecipeService/TestExecutionService/ProductService/TestDefinitionService/ReportService/
   DeviceModeController/DeviceWritePipeline）。
5. Headless：App 启动守卫（组合根每进程只初始化一次）；Avalonia.Headless 11.3.9 多 AvaloniaFact
   会话在此环境仍偶发挂起，Headless 保持单端到端事实，并写入 build-and-test.ps1 自动重试。
6. 工程化：tools\build-and-test.ps1（Release 构建 + 分项目测试 + Headless 重试）、
   tools\publish-windows.ps1（win-x64 框架依赖/自包含可选）。

## 测试
- Core 53、Integration 15、App 9、Headless 1（Debug 合计 78）；Release 以 build-and-test.ps1 为准。
