# 阶段 2 基础骨架与持久化：构建与测试证据

- 日期：2026-09-01
- 源码根：D:\易峥\2026\2026-09\Avalonia_上位机通用模板\项目代码\XXX.TestBench.Template
- 解决方案：XXX.TestBench.Template.sln（net8.0；Avalonia 11.3.9；Microsoft.Data.Sqlite 8.0.10）

## 1. Release 全解决方案构建

命令：`dotnet build XXX.TestBench.Template.sln -c Release`

结果：0 警告 / 0 错误（9 个项目：App/Core/Infrastructure/Devices + 4 个测试项目）。

## 2. 测试结果（Release）

命令：`dotnet test XXX.TestBench.Template.sln -c Release`

| 项目 | 通过 | 失败 | 说明 |
| --- | --- | --- | --- |
| XXX.TestBench.Core.Tests | 25 | 0 | 认证（成功/失败锁定/改密/会话撤销）、配方（草稿/校验/发布/复制/权限）、任务（发布配方匹配/记录快照/状态机/权限）、写入安全链（权限/模式/连接/活动任务/风险确认/只读/审计）、模式控制（Hardware 失败保留模式不回退） |
| XXX.TestBench.Integration.Tests | 11 | 0 | SQLite schema+种子+重启回读、admin PBKDF2 校验、配置加载/校验/原子保存备份、Simulation 读写与仿真标识、Hardware 工厂拒绝、模式控制器集成、全链路闭环（产品→项点→配方→发布→任务→启动→完成→重启回读）、UoW 事务 |
| XXX.TestBench.App.Tests | 6 | 0 | 外壳状态（模式徽标/故障诊断/连接状态）、组合根成功与 Faulted 路径 |
| XXX.TestBench.App.Headless.Tests | 2 | 0 | Avalonia Headless 外壳绑定：仿真徽标可见、故障横幅可见 |

## 3. 阶段 2 退出条件核对

- [x] Core/Infrastructure/Devices/App 独立构建，全解决方案 Release 0 警告 0 错误
- [x] 登录（AuthenticationService + 失败锁定 + 首次强制改密 + 会话撤销）测试通过
- [x] 配置加载/校验/原子保存测试通过
- [x] SQLite 正式 schema 初始化、种子、完整性检查、重启回读测试通过
- [x] 未开始任何 P0 业务页面编码（等待阶段 1 UI 详细规格）
- [x] Hardware 失败保留 Hardware 模式进入 Faulted，不自动回退 Simulation（单元+集成+控制器三层证据）

## 4. 说明与后续

- 会话管理为进程内存实现（InMemorySessionManager），重启后需重新登录；落库按需在后续阶段引入。
- 阶段 1 开放问题（任务编号/产品标识字段/参数类型/角色权限矩阵等）仍按 0002 决策记录中的临时假设实现，未锁定为最终规格。
- UI 外壳仅为结构骨架（顶栏/左导航/内容区/底部状态 + 故障横幅），不含业务页面。
